export const mutations = {
	setWord(state, word) {
		state.word = word;
	},
	setTopics(state, topics) {
		state.topics = topics;
	},
	setGender(state, gender) {
		state.gender = gender;
	},
	setAges(state, ages) {
		state.ages = ages;
	},
	setPlaces(state, places) {
		state.places = places;
	},
};
